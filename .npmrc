# Require packages to have been published for at least 3 days before
# they can be installed. Reduces exposure to supply chain attacks.
# Requires npm >=11.10.0; silently ignored by older versions.
# See: https://socket.dev/blog/npm-introduces-minimumreleaseage-and-bulk-oidc-configuration
min-release-age=3
